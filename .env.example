# ==============================================================================
# Clipder — Environment Variables
# Copy this file to .env and fill in your values.
# ==============================================================================

# ---- DATABASE (REQUIRED) ----
# PostgreSQL connection string using the asyncpg driver.
# Example: postgresql+asyncpg://user:password@localhost:5432/clipder
DATABASE_URL=

# ---- JWT AUTH (REQUIRED) ----
# Generate a strong random key: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=
# Algorithm and token lifetime (optional, defaults shown)
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# ---- TWITCH API (REQUIRED) ----
# Create an app at https://dev.twitch.tv/console/apps
TWITCH_CLIENT_ID=
TWITCH_CLIENT_SECRET=
# OAuth callback — MUST match exactly what is set in your Twitch Developer Console app.
# Use port 3000 (Vite) in local dev so the redirect is proxied through to the backend.
# In production set this to your real backend/frontend callback URL.
TWITCH_REDIRECT_URI=http://localhost:3000/api/v1/auth/twitch/callback

# ---- TWITCH CONTENT (REQUIRED) ----
# Comma-separated channel names to fetch clips from
TWITCH_CHANNELS=
# Comma-separated Twitch category names
TWITCH_CATEGORIES=Just Chatting,VALORANT,League of Legends

# ---- GROQ AI (REQUIRED for AI features) ----
# Get a key at https://console.groq.com
GROQ_API_KEY=
# Cloud chat model on Groq (not a local laptop model). Override if Groq retires a name.
GROQ_CHAT_MODEL=llama-3.3-70b-versatile

# ---- CORS (optional) ----
# Frontend origin for CORS. Default: http://localhost:3000
FRONTEND_URL=http://localhost:3000

# ---- SUPABASE (optional) ----
SUPABASE_URL=
SUPABASE_ANON_KEY=

# ---- OPUS CLIP (optional) ----
OPUS_CLIP_API_KEY=
